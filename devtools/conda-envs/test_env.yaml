name: test
channels:
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov
  - scikit-learn
  - joblib
  - numpy
  - networkx
    # Pip-only installs
  #- pip:
  #  - codecov

